#+PROPERTY: header-args:python :session learn20 :async
* Non-linear feature engineering for Logistic Regression

** Set up
*** Moons
#+begin_src python
import numpy as np
import pandas as pd
from sklearn.datasets import make_moons

feature_names = ["Feature #0", "Feature #1"]
target_name = "class"

X, y = make_moons(n_samples=100, noise=0.13, random_state=42)

# We store both the data and target in a dataframe to ease plotting
moons = pd.DataFrame(
    np.concatenate([X, y[:, np.newaxis]], axis=1),
    columns=feature_names + [target_name],
)
data_moons, target_moons = moons[feature_names], moons[target_name]
#+end_src

*** Gaussian
#+begin_src python
from sklearn.datasets import make_gaussian_quantiles

X, y = make_gaussian_quantiles(
    n_samples=100, n_features=2, n_classes=2, random_state=42
)
gauss = pd.DataFrame(
    np.concatenate([X, y[:, np.newaxis]], axis=1),
    columns=feature_names + [target_name],
)

data_gauss, target_gauss = gauss[feature_names], gauss[target_name]

#+end_src

*** XOR
#+begin_src python
xor = pd.DataFrame(
    np.random.RandomState(0).uniform(low=-1, high=1, size=(200, 2)),
    columns=feature_names,
)
target_xor = np.logical_xor(xor["Feature #0"] >0, xor["Feature #1"] > 0)
target_xor = target_xor.astype(np.int32)
xor['class'] = target_xor
data_xor = xor[feature_names]
xor
#+end_src

** Visualization of the data
#+begin_src python :results graphics file :file ../img/learn20_plot1.png
import matplotlib.pyplot as plt
from matplotlib.colors import ListedColormap

plt.clf()
_, axs = plt.subplots(ncols=3, figsize=(14, 4), constrained_layout=True)

common_scatter_plot_params = dict(
    cmap=ListedColormap(["tab:red", "tab:blue"]),
    edgecolor="white",
    linewidth=1,
)

axs[0].scatter(
    data_moons[feature_names[0]],
    data_moons[feature_names[1]],
    c=target_moons,
    ,**common_scatter_plot_params,
)
axs[1].scatter(
    data_gauss[feature_names[0]],
    data_gauss[feature_names[1]],
    c=target_gauss,
    ,**common_scatter_plot_params,
)
axs[2].scatter(
    data_xor[feature_names[0]],
    data_xor[feature_names[1]],
    c=target_xor,
    ,**common_scatter_plot_params,
)
axs[0].set(
    title="The moons dataset",
    xlabel=feature_names[0],
    ylabel=feature_names[1],
)
axs[1].set(
    title="The Gaussian quantiles dataset",
    xlabel=feature_names[0],
)
axs[2].set(
    title="The XOR dataset",
    xlabel=feature_names[0],
)
plt.gcf()
#+end_src

** Fuction for plotting desicion boundary
#+begin_src python
from sklearn.inspection import DecisionBoundaryDisplay


def plot_decision_boundary(model, title=None):
    datasets = [
        (data_moons, target_moons),
        (data_gauss, target_gauss),
        (data_xor, target_xor),
    ]
    plt.clf()
    fig, axs = plt.subplots(
        ncols=3,
        figsize=(14, 4),
        constrained_layout=True,
    )

    for i, ax, (data, target) in zip(
        range(len(datasets)),
        axs,
        datasets,
    ):
        model.fit(data, target)
        DecisionBoundaryDisplay.from_estimator(
            model,
            data,
            response_method="predict_proba",
            plot_method="pcolormesh",
            cmap="RdBu",
            alpha=0.8,
            # Setting vmin and vmax to the extreme values of the probability to
            # ensure that 0.5 is mapped to white (the middle) of the blue-red
            # colormap.
            vmin=0,
            vmax=1,
            ax=ax,
        )
        DecisionBoundaryDisplay.from_estimator(
            model,
            data,
            response_method="predict_proba",
            plot_method="contour",
            alpha=0.8,
            levels=[0.5],  # 0.5 probability contour line
            linestyles="--",
            linewidths=2,
            ax=ax,
        )
        ax.scatter(
            data[feature_names[0]],
            data[feature_names[1]],
            c=target,
            ,**common_scatter_plot_params,
        )
        if i > 0:
            ax.set_ylabel(None)
    if title is not None:
        fig.suptitle(title)
#+end_src

** LogisticRegression with StandardScaler
#+begin_src python
from sklearn.pipeline import make_pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.linear_model import LogisticRegression

logistic_regression = make_pipeline(StandardScaler(), LogisticRegression())
logistic_regression
#+end_src


#+begin_src python :results graphics file :file ../img/learn20_plot2.png
plot_decision_boundary(logistic_regression, title="Linear classifier")
plt.gcf()
#+end_src

** LogisticRegression with KBinsDiscretizer
#+begin_src python
from sklearn.preprocessing import KBinsDiscretizer

classifier = make_pipeline(
    KBinsDiscretizer(n_bins=5, encode="onehot"),
    LogisticRegression(),
)
classifier
#+end_src

#+begin_src python :results graphics file :file ../img/learn20_plot3.png
plot_decision_boundary(classifier, title="Binning classifier")
plt.gcf()
#+end_src

** LogisticRegression with SplineTransformer
#+begin_src python
from sklearn.preprocessing import SplineTransformer

classifier = make_pipeline(
    SplineTransformer(degree=3, n_knots=5),
    LogisticRegression(),
)
classifier
#+end_src

#+begin_src python :results graphics file :file ../img/learn20_plot4.png
plot_decision_boundary(classifier, title="Spline classifier")
plt.gcf()
#+end_src

** LogisticRegression with StandardScaler and PolynomialFeatures
#+begin_src python
from sklearn.preprocessing import PolynomialFeatures

classifier = make_pipeline(
    StandardScaler(),
    PolynomialFeatures(degree=3, include_bias=False),
    LogisticRegression(C=10),
)
classifier
#+end_src

#+begin_src python :results graphics file :file ../img/learn20_plot5.png
plot_decision_boundary(classifier, title="Polynomial classifier")
plt.gcf()
#+end_src

** LogisticRegression with StandardScaler and Nystroem (poly)
#+begin_src python
from sklearn.kernel_approximation import Nystroem

classifier = make_pipeline(
    StandardScaler(),
    Nystroem(kernel="poly", degree=3, coef0=1, n_components=100),
    LogisticRegression(C=10),
)
classifier
#+end_src

#+begin_src python :results graphics file :file ../img/learn20_plot6.png
plot_decision_boundary(classifier, title="Polynomial Nystroem classifier")
plt.gcf()
#+end_src

** LogisticRegression with StandardScaler and Nystroem (rbf)
#+begin_src python
classifier = make_pipeline(
    StandardScaler(),
    Nystroem(kernel="rbf", gamma=1, n_components=100),
    LogisticRegression(C=5),
)
classifier
#+end_src

#+begin_src python :results graphics file :file ../img/learn20_plot7.png
plot_decision_boundary(classifier, title="RBF Nystroem classifier")
plt.gcf()
#+end_src

** LogisticRegression with KBinsDiscretizer and Nystroem (rbf)
#+begin_src python
classifier = make_pipeline(
    KBinsDiscretizer(n_bins=5),
    Nystroem(kernel="rbf", gamma=1.0, n_components=100),
    LogisticRegression(),
)
classifier
#+end_src

#+begin_src python :results graphics file :file ../img/learn20_plot8.png
plot_decision_boundary(classifier, title="Binning + Nystroem classifier")
plt.gcf()
#+end_src

** LogisticRegression with SplineTransformer and Nystroem (rbf)
#+begin_src python
from sklearn.kernel_approximation import Nystroem

classifier = make_pipeline(
    SplineTransformer(n_knots=5),
    Nystroem(kernel="rbf", gamma=1.0, n_components=100),
    LogisticRegression(),
)
classifier
#+end_src

#+begin_src python :results graphics file :file ../img/learn20_plot9.png
plot_decision_boundary(classifier, title="Spline + RBF Nystroem classifier")
plt.gcf()
#+end_src
